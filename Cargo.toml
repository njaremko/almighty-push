[package]
name = "almighty-push"
version = "0.1.0"
edition = "2021"
description = "Push jj stacks to GitHub as PRs"

[dependencies]
anyhow = "1.0"
chrono = "0.4"
clap = { version = "4.5", features = ["derive"] }
regex = "1.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"